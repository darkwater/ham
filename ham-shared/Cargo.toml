[package]
name = "ham-shared"
version = "0.1.0"
edition = "2024"

[dependencies]
serde      = { version = "1.0.188", features = ["derive"] }
serde_json = "1.0.149"

sqlx  = { version = "0.9.0-alpha.1", optional = true }

[features]
default = []
sqlx = ["dep:sqlx"]
